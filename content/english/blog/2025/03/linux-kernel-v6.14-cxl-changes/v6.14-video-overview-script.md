# YouTube Short Script: Linux v6.14 CXL Changes

**Kernel range:** v6.13 → v6.14  
**Target length:** 60–90 seconds  
**Generated:** 2026-06-22

---

[Text overlay: "Linux v6.14 CXL Changes"] Linux kernel 6.14 is out — here's what changed for CXL in under 90 seconds.

[Text overlay: "13 commits merged"] Thirteen commits landed across the CXL and DAX subsystems this cycle. Not a huge release, but the quality is high.

[Show commit title: "CXL spec rev 3.1 event updates"] The biggest theme: CXL spec revision 3.1 compliance for event records. The General Media, DRAM, Memory Module, and Common Event Record structures were all updated to match the latest spec. If you're consuming CXL error and health events, this affects your tooling.

[Text overlay: "New: Component Identifier"] Alongside those updates, Component Identifier formatting was added for spec rev 3.1 events — making it easier to trace events back to a specific device component.

[Show commit title: "cxl/pci: Add CXL Type 1/2 support"] On the PCI side, Type 1 and Type 2 device support was added to the DVSEC range register decoder — expanding CXL beyond Type 3 memory devices.

[B-roll: kernel code] Two cleanup commits trimmed dead code in the PMEM path, removing a bridge-matching helper that was replaced by a generic driver-core API.

[Text overlay: "1 bug fix"] And one bug fix corrected a function prototype in driver core that CXL depends on.

[End card with blog URL] Full details and the complete commit list are in the blog post — link in description.
